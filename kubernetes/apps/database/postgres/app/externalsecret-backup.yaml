---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cnpg-backup-azure
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: cnpg-backup-azure-creds
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: cnpg-backup-azure
