---
# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: Forgejo OAuth2 Provider and Application
context:
  authorization_flow:
    !Find &authorization_flow [
      authentik_flows.flow,
      [slug, default-provider-authorization-implicit-consent],
    ]
  invalidation_flow:
    !Find &invalidation_flow [
      authentik_flows.flow,
      [slug, default-provider-invalidation-flow],
    ]
  property_mappings: &property_mappings
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
    - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
  signing_key:
    !Find &signing_key [
      authentik_crypto.certificatekeypair,
      [name, authentik Self-signed Certificate],
    ]
entries:
  # OAuth2 Provider for Forgejo
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: forejo
    attrs:
      name: forejo
      authorization_flow: *authorization_flow
      invalidation_flow: *invalidation_flow
      client_type: confidential
      client_id: !Env FOREJO_CLIENT_ID
      client_secret: !Env FOREJO_CLIENT_SECRET
      redirect_uris:
        - url: "https://git.melotic.dev/user/oauth2/authentik/callback"
          matching_mode: strict
      property_mappings: *property_mappings
      signing_key: *signing_key

  # Application for Forgejo
  - model: authentik_core.application
    identifiers:
      slug: forejo
    attrs:
      name: Forgejo
      slug: forejo
      provider:
        !Find [authentik_providers_oauth2.oauth2provider, [name, forejo]]
      policy_engine_mode: any
      meta_icon: https://raw.githubusercontent.com/walkxcode/dashboard-icons/main/svg/forgejo.svg
      meta_description: Self-hosted Git service
      meta_publisher: Forgejo
      open_in_new_tab: true
