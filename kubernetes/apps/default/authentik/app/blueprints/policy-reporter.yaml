# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
version: 1
metadata:
  name: policy-reporter Proxy Provider and Application
context:
  authorization_flow:
    !Find &authorization_flow [
      authentik_flows.flow,
      [slug, default-provider-authorization-implicit-consent],
    ]
  invalidation_flow:
    !Find &invalidation_flow [
      authentik_flows.flow,
      [slug, default-provider-invalidation-flow],
    ]
entries:
  - model: authentik_providers_proxy.proxyprovider
    identifiers:
      name: policy-reporter
    attrs:
      name: policy-reporter
      authorization_flow: *authorization_flow
      invalidation_flow: *invalidation_flow
      mode: forward_single
      external_host: https://policy-reporter.melotic.dev
  - model: authentik_core.application
    identifiers:
      slug: policy-reporter
    attrs:
      name: policy-reporter
      slug: policy-reporter
      provider: !Find [authentik_providers_proxy.proxyprovider, [name, policy-reporter]]
      policy_engine_mode: any
      meta_description: Policy report dashboard
      open_in_new_tab: true
