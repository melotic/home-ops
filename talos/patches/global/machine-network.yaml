# yaml-language-server: $schema=https://www.talos.dev/v1.11/schemas/config.schema.json
machine:
  features:
    hostDNS:
      enabled: true
      forwardKubeDNSToHost: false
  network:
    disableSearchDomain: true
    nameservers:
      - 10.60.0.1
